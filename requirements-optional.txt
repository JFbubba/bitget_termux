# Dépendances OPTIONNELLES — le système fonctionne SANS elles (il dégrade
# proprement). Elles débloquent des fonctions supplémentaires.
# Sur le VPS :  pip install -r requirements-optional.txt
#
# yfinance : macro_data.py — contexte TradFi (VIX/DXY/SPX/10Y/or/pétrole),
#            commande /tradfi + agent macro du cerveau plus réactif.
#            (tire pandas/numpy comme sous-dépendances)
yfinance
#
# ccxt : ccxt_markets.py — prix spot + funding/OI multi-exchange (100+ venues),
#        commande /cross. Méthodes de LECTURE uniquement.
ccxt

# book_collector.py — collecteur microstructure HAUTE FIDÉLITÉ via WebSocket public
# Bitget (canaux books15 + trade). OPTIONNEL : sans lui, le REST-poll de
# microstructure.py reste disponible. Flux public, lecture seule, aucune clé.
# RÉUTILISÉE par ws_orders.py (T4, §11 SAVOIR) — canal WS PRIVÉ `orders`, LECTURE
# SEULE (observation pure, aucun ordre). Déjà installée sur cette machine.
websocket-client>=1.6.0

# scikit-learn : strategy_lab.strat_random_forest (§72, algo classique n°10) —
#                stratégie ML causale du laboratoire (refit périodique sur le seul
#                passé). Sans lui : la stratégie est INERTE ([0]), rien ne casse.
#                (installé sur le VPS le 06/07 via pip --break-system-packages,
#                comme torch §65)
scikit-learn

# pytest : suite unitaire de dev tests/ (banc COMPLÉMENTAIRE — tests_audit.py reste
#          la porte officielle). Outil de dev pur : jamais requis au runtime du bot.
#          Sur ce VPS (PEP 668) : venv jetable, ex.
#          python3 -m venv .venv && .venv/bin/pip install pytest && .venv/bin/pytest
pytest
