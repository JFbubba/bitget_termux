---
source: package/_scratch/
category: bitget-tooling
action: skipped
target: —
---

## Contenu
- `bitget_*` (analyses, tests, klines), `strategy_ai_agents_bitget_v*.py`,
  `user_input_files_*`, `stop_grid_module*.py`, `TimeBasedNAS_EA.mq5`,
  `btc_*.json` (snapshots de marché).

## Valeur extraite
- Brouillons + résultats d'exécution stockés ; pas une source de vérité.
- `strategy_ai_agents_bitget_v4.py` est éventuellement intéressant (probablement
  l'ancêtre du système d'agents actuel) — à ouvrir si on cherche un comportement
  perdu.

## Cible d'intégration
- `skipped` par défaut.
- Si besoin d'archéologie sur l'ancienne version v4 → ouverture ponctuelle, sinon
  rien.
