---
source: package/AI-Trader/
category: agent-architecture
action: pending
target: (à évaluer après lecture du README)
---

## Contenu
- `README.md` (EN) + `README_ZH.md` (中文) — projet bilingue.
- Stack Node/TypeScript : `package.json`, `tsconfig.json`, `service/`, `skills/`,
  `docs/`, `assets/`.

## Valeur extraite
- À évaluer : si c'est une distrib AI-trader notable (avec skills LLM Anthropic),
  voir si `skills/` apporte des patterns réutilisables côté `assistant/`.
- Sinon, juste une référence externe.

## Cible
- Lire `README.md` pour décider entre `tool-adapted` (extraire 1-2 patterns de
  `skills/`) ou `skipped`.
