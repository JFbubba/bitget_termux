---
source: package/bitget-skill/
category: bitget-tooling
action: pending
target: assistant/tools.py (si skill LLM réutilisable)
---

## Contenu
- `references/` (à explorer).
- (autrement quasi vide à ce niveau).

## Valeur extraite
- Probablement un **Claude skill** lié à Bitget — vérifier `references/` pour
  voir s'il y a une `SKILL.md` ou un manifeste.

## Cible
- Si c'est une skill structurée et utile (lecture compte, lecture marché), à
  comparer à ce qu'on a déjà côté `assistant/` ou MCP. Sinon `skipped`.
