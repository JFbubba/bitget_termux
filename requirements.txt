# Dependances Python runtime du Bitget Termux Agent (paper / dry-run only).
#
# Audit AST du depot : TROIS dependances tierces sont reellement importees par
# le code. Tout le reste appartient a la bibliotheque standard Python.
#
#   requests      -> appels HTTP (API publique Bitget + Telegram)
#   python-dotenv -> chargement du .env local (jamais versionne)
#   numpy         -> calcul matriciel des agents quantitatifs (HMM/Mahalanobis/
#                    Monte-Carlo) : simons_agent, savant_agent, futuretester,
#                    evolution, strategy_lab. Sans lui, ces agents retombent
#                    gracieusement en neutre (try/except) mais NE contribuent pas.
#
# Installation (Termux ou poste local) :
#   pip install -r requirements.txt
#
# NE PAS executer "pip install --upgrade pip" sous Termux (instable/interdit).
requests>=2.31.0
python-dotenv>=1.0.0
# numpy BORNÉ <2 (ERR-004) : la pile système du VPS (matplotlib/scipy apt) est
# compilée contre NumPy 1.x — un numpy 2.x installé par le repli d'update_vps.sh
# sur machine reconstruite casserait le bot. Pivot attesté 08/07 : numpy 1.26.4.
numpy>=1.24.0,<2
