
# Claude/export local
# Local archives / broken files
# Logs / journals
# Node dependencies
# Python cache
# Runtime CSV / backups
# Runtime PID / reports
# Runtime state
# Secrets / local auth
*.bak.*
*.broken.*
*.corrupted.*
*.csv
*.jsonl
*.log
*.pyc
*.tar.gz
*_backup.py
*_backup_*.csv
*_old.csv
.env
.env.*
!.env.example
brain_log.json
brain_weights.json
conversation_state.json
.runtime_cache.json
__pycache__/
agent_loop.pid
claude_export/
final_outcomes_journal.csv
node_modules/
open_outcomes_state.csv
open_outcomes_state_backup_*.csv
order_signals_report.txt
outcomes_journal*.csv
paper_positions.json
pending_orders.json
signals_journal.csv
telegram_offset.txt
strategies_out/
.microstructure_buffer.json
accumulation_ledger.json
accumulation_real_ledger.json
futures_real_ledger.json
.accum_spend_alert.json
.market_timing_alert.json
.learning_health_alert_state.json
.carry_journal.json

# Claude Code : hooks locaux (machine-specifique, contient des mots-cles d'ordre
# pour le garde-fou ; ne doit pas etre suivi sinon safe_push_check les detecte)
.claude/hooks/

# Outil externe Agent Hub (bgc) : a son propre depot git, ne pas l'embarquer
agent_hub/
# Artefact runtime (rapport de validation des agents, regenere)
validation_report.json
.futures_pos_state.json
brain_hitrates.json
data_history/
xs_paper_journal.jsonl
.xs_paper_etat.json

# Runtime state — garde-fou stop & supervision (regenere a chaud, machine-specifique)
.stop_guardian_heartbeat.json
.watchdog_heal_state.json
.failsafe_state.json
logs/

# Config MCP locale (Claude Code) — chemins absolus VPS, outillage dev, hors runtime bot
.mcp.json

# ledger de coût LLM (runtime, idée #3)
.llm_cost_ledger.jsonl

# mémoire de situations (runtime, idée #6)
.situation_memory.jsonl

# poids du réseau neuronal de fusion (runtime binaire, §65) — entraînés sur le VPS
neural_net_weights.pt
neural_net_meta.json
neural_net_pretrained.pt
.liquidity_journal.jsonl
.overlay_votes.jsonl
.alt_carry_state.json
.alt_carry_journal.jsonl
.mm_state.json
.mm_journal.jsonl
.xs_promotion_alerted
.trades_archive.jsonl
.daily_digest_stamp
trading_real_ledger.json
.mm_lab_result.json

# Venv du prototype QML (qml_prototype)
.venv/

# Collecteur de données (data_collector) : artefacts locaux, jamais committés
data_collector/raw_items.jsonl
data_collector/sorted_items.jsonl
data_collector/categories.json
data_collector/sorter_state.json

# Graphe généré par graphify (régénérable, ne pas committer)
graphify-out/
futures_open.lock
futures_pending.json
futures_pending.json.tmp
accumulation_ledger_unreliable.flag
accumulation_real_ledger.json.tmp
trading_ledger_unreliable.flag
trading_real_ledger.json.tmp
