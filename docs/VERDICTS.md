# VERDICTS — registre des idées testées et de leur sort

**But** : source unique de « qu'est‑ce qui a été essayé, et avec quel résultat ». À
CONSULTER avant de (re)tester une idée — pour ne JAMAIS re‑mesurer ce qui est déjà mort.
Chaque ligne : preuve mesurée, pas opinion. Mettre à jour à chaque verdict rendu.

Statuts : **REJETÉ** (mesuré perdant, ne pas re‑tester) · **GARDÉ** (branché, mesuré gagnant) ·
**MUET** (branché mais neutralisé par une porte, mesure en ombre) · **VIVANT** (piste ouverte,
à re‑mesurer) · **ÉVALUÉ** (service/donnée jugé sans mesure d'edge).

## REJETÉ — mesuré perdant, NE PAS re‑tester
| Idée | Verdict | Preuve | Réf |
|---|---|---|---|
| MVRV Z-score on-chain (signal directionnel BTC) | REJETÉ (directionnel) — beta de cycle ; tilt DCA réel mais redondant | 19/07 `mvrv_lab.py` (LECTURE SEULE, défaut OFF, Coin Metrics community KEYLESS, realized_cap DÉRIVÉ = mktcap/MVRV car CapRealUSD Pro-only ; PriceUSD 2013+ ~4 cycles, cross-check Bitget D1 Spearman 0,9993). CAUSAL (expanding) : IC(z,fwd)≈0 (h7 +0,022 t0,56 / h30 +0,011) ; directionnel sign(−z)·fwd Sharpe −0,16 SOUS B&H +0,23 ; **DSR 0,002 ≪ 0,95** ; walk-forward OOS change de SIGNE (t −0,29). Réversion classique seulement en normalisation full-sample NON causale (look-ahead, t≈−2 sur ~3 cycles). NE PAS brancher au cerveau/futures. Tilt DCA coût moyen = RÉEL (−16 à −30 % vs DCA plat, causal, robuste) mais REDONDANT avec `accumulation_engine.opportunity_score` (drawdown/RSI/MA/fear) + beta de cycle → dormant, opt-in `MVRV_TILT_ENABLED` défaut OFF non câblé. | `mvrv_lab.py` · `docs/MVRV_LAB.md` · ERR-001 |
| Wyckoff « climax de volume » (SC/BC + spring/upthrust intrabar, look-ahead-free) | REJETÉ — réel-non-tradable | 19/07 `wyckoff_lab.py` (LECTURE SEULE, défaut OFF) : 8 majors × échelle M1..W1, taker+maker, N_trials=480. Effet directionnel FAIBLE mais réel (permutation p=0,004 à 4H, OOS+ t=3,28, bat B&H) MAIS gate 2/5 : **DSR 0,054<0,95** (SR gagnant 0,264 < SR0 0,423), t_HAC 2,43<3, non cohérent 2 TF adjacents. Intraday M1–M30 = fee-killed taker (prior confirmé) ; H1–4H = tué par la DÉFLATION (pas les frais) ; D1/W1 = small-sample. Contrôles+ OK (réversion 1h t=6,75, oracle t=44). Seul angle Wyckoff mesurable (la méthode = même piège que SMC/ICT, cf. `docs/WYCKOFF.md`). Rouvrir seulement en univers 100+ coins OU input d'ensemble OU frais≈0. | `wyckoff_lab.py` · `docs/WYCKOFF_LAB.md` · ERR-014 |
| Figures chartistes classiques (H&S/inverse, double/triple top-bottom, triangles asc/desc/sym, wedges, rectangle, flags — 16 figures, look-ahead-free, complément de Wyckoff) | REJETÉ — réel-non-tradable ; **confluence indicateurs = seul levier, et seulement vers 0** | 19/07 `chart_patterns.py` (LECTURE SEULE, défaut OFF, greffé sur `price_action.swing_points`, mesuré par le harnais `wyckoff_lab`). 8 majors × M1..W1 × 16 figures × 5 h × 4 confluences, N_trials=2560. Figure gagnante `sym_triangle_long` 1D h=1 full-confluence : net maker +162 bps t_HAC 2,51 n=57 MAIS **DSR 0,138≪0,95** (écrasée par la déflation) ; les gros chiffres (inverse_hs 1D +183, bear_flag 1W +1084 bps) = **small-sample haute-TF n=30-35** (piège Bulkowski/LMW). Gate 0/3. **Effet confluence MESURÉ** (réponse à « les indicateurs renforcent la confirmation ») : net maker moyen pondéré none −1,24 → volume −0,12 → full −0,09 bps → les indicateurs **réduisent la perte** (filtrent les pires cassures, intuition JUSTE) mais **ne basculent pas** positif ; confluence Wyckoff-climax trop rare (−10,88 bps, n petit). Libs publiques (`PatternPy`/`TradingPatternScanner`) inutilisables (géométrie `.shift(-1)` = barre future ; `*_tech.py` lisse en centré = fuite). Détecteur gardé = instrument de mesure + ancrage Grok (`cross_wyckoff.objective_patterns`, `agreement.pattern_overlap`). Rouvrir si frais≈0 OU univers 100+ coins OU input d'ensemble. | `chart_patterns.py` · `docs/CHART_PATTERNS.md` · ERR-014 |
| SMC comme aide au PLACEMENT maker (la BONNE lentille, ERR-016 : exécution, PAS direction) — un fill maker à un niveau de structure (FVG/BPR/niveau balayé, bon côté) a-t-il un meilleur MARKOUT qu'un fill naïf ? | REJETÉ — n'améliore PAS l'exécution (prior tenu) | 19/07 `smc_execution_lab.py` (LECTURE SEULE, défaut OFF, mirror de `vpin_lab`, `microstructure.markout` réutilisé, data_history 8 majors × M1..W1 × h{3,6,12}, N_trials=192, DSR déflatée + bootstrap + non-chevauchant). **0 essai robuste, non cohérent 2 TF.** Le « meilleur » delta (BTC 1W h=12 +2000 bps, t=3,66) = artefact SMALL-N haute-TF (n_at=43, markout 12 semaines) **tué par DSR=0,58<0,95** ; tous les gros deltas positifs sont 1D/1W n<60 (même piège que chart_patterns/wyckoff). Là où n est FIABLE (intraday, grand n), l'effet est ≈0 voire NÉGATIF : BTC 1H delta −5 à −7 bps (t~−1,7) = **sélection adverse** (les FVG/niveaux balayés = liquidité qui se fait courir, fill-and-be-run). ⚠️ RECADRAGE ERR-017 : ce verdict juge un **SIGNAL de placement CONCRET** (poser un limit maker au bord d'un FVG/OTE/niveau balayé) — CE signal n'est pas robuste. Ce n'est PAS un verdict sur « SMC la méthode » : une méthode ne s'évalue pas à l'edge (ni directionnel ni exécution), elle se VÉRIFIE (reconnaît-elle correctement). SMC comme MÉTHODE reste un contexte de lecture valable (dashboard), non un edge. Construire ce lab pour « mesurer l'edge du SMC » était lui-même une instance d'ERR-017 ; ce qui reste valide = le résultat du signal de placement ci-dessus. | `smc_execution_lab.py` · `docs/SMC_EXECUTION_LAB.md` · ERR-016/017 |
| ICT 2022 « top-down + NY killzone » (les 2 angles morts jamais testés : biais HTF strict D1+4H + filtre session NY equity-overlap 08:30-11:00 ET, via lib `smartmoneyconcepts`, look-ahead corrigé) | REJETÉ — confirmé, réel-non-tradable | 19/07 `scratchpad/smc_relab/` (venv smc, causalité PROUVÉE par troncature = 0 divergence, `available_at=pivot+3·swing`). Modèle canonique complet (Unicorn OB∩FVG + OTE + discount + NY) = **0 trade** (OB de la lib ultra-rare, 73-86 sur ~40k barres). Core POOLED : net maker **−25 bps 24/7 (t_HAC −3,02)** ; **le filtre NY AGGRAVE** (−53,5 bps, t −3,68) au lieu d'aider → 2ᵉ angle mort réfuté. Best grille 48 configs : Sharpe −0,19, **DSR 0,000 (0/5 majors, SR0 0,66)**. Walk-forward OOS : **−33,3 bps maker (t −2,86)** / taker −38 (t −3,32). B&H −39 % (régime baissier) et le modèle short-heavy perd quand même ; brut déjà ~−20 bps → **anti-prédictif, PAS qu'un pb de frais**. Caveat honnête : régime ~1 an baissier (le prior couvre autres régimes + actifs décorrélés déjà réfutés). Les 2 angles morts testés proprement ne récupèrent AUCUN edge. **NE PAS relancer.** | `scratchpad/smc_relab/` · [[smc-aio-rejected]] · ERR-014 |
| Grid trading (grille à INVENTAIRE non‑directionnelle, spot ATR‑adaptative géométrique) | REJETÉ — **fee‑killed ET breakout‑trap‑killed** | **18/07** banc dédié `grid_lab.py` (LECTURE SEULE, défaut OFF) : synthèse optimisée d'un traité (espacement GÉOMÉTRIQUE, bornes VWAP±k·ATR, filtre régime ADX<22+Bollinger stable+volume, COUPE sur cassure, règle d'or ≥3×coûts, comptabilité TOTAL‑P&L = grid+latent−frais, cap anti‑martingale). Mesuré BTC+ETH (+SOL vérifié) sur échelle COMPLÈTE M1..W1, frais maker **8 bps/côté (BGB spot)**, sélection TRAIN→OOS, PBO+DSR+walk‑forward+stress de coûts ×{1,1.5,2}+benchmark B&H apparié. **0/16 (sym,TF) survivent** (0/2 SOL) : DSR<0,95 PARTOUT (best ≈0,55), OOS Sharpe ≈0/négatif, folds+≤0,4. Rares TOTAL positifs = artefacts (BTC W1 +23,64$ mais PBO 0,71 surappris/DSR 0,29). Sur D1 le **grid‑profit lui‑même négatif** (BTC −18,68/ETH −2,98/SOL −71,32) : les coupes forcées réalisent le piège de la cassure (pas qu'un pb de frais — nature range‑only punie par les tendances). Là où grid‑profit>0 (ETH H4 +5,52) les frais l'annulent (5,35, net +0,17, OOS −3,03). Le maker NE fait basculer AUCUNE config (le churn seed+coupe taille les fills). Mythe corrigé : « écartée car exige des annulations » = CADUC (le MM annule déjà ; Trade‑only interdit les retraits, pas les annulations) — c'est ÉCONOMIQUE. Rouvrir seulement si frais≈0 (VIP) ou actif genuinement range‑bound. | `grid_lab.py` · `docs/GRID_STRATEGIES.md` · ERR‑001 |
| SMC / ICT (FVG + Order Blocks + sweep + CHoCH) | REJETÉ — **RE‑TESTÉ CORRECTEMENT** (méthode d'abord suspecte ERR‑014, maintenant confirmée) | Ancien rejet reposait sur une checklist de confluence CONTEMPORAINE (`smc.py:442`) — suspect ERR‑014. **Re‑test 18/07** en MACHINE À ÉTATS CAUSALE (`scratchpad/strategy_tester/smc_ict.py` : sweep→swing responsable→CHoCH displacement→FVG→entrée décalée sur retest→stop structurel ; **causalité VÉRIFIÉE** par troncature = aucun look‑ahead), 6 sym × 8 TF, taker+maker, benchmark B&H. Verdict en 3 temps : (1) ma 1ʳᵉ conclusion (« perd des 2 côtés ») utilisait la PIRE variante (TP à RR fixe) = ERR‑014 bis ; (2) modèle COMPLET (target structurel + entrée OTE dans le discount, `entry_depth`) a un edge brut **réel mais LOCALISÉ** aux majors 4H (BTC +13,7 / ETH +56,6 bps/trade brut — retrouve l'ancien `ict_model.py` perdu au reset) ; (3) mais sur l'échelle COMPLÈTE il ne généralise PAS : perdant médian même en maker (PF méd 0,61, Sharpe −0,62, PnL long ET short négatifs, bat B&H 7/46). L'ingrédient décisif = le target structurel (+13,7 vs −75,9 bps RR fixe). **REJETÉ comme edge général** (non‑généralisation, effet majors/4H/régime), cohérent avec le test décorrélé déjà fait (`ob_decorrel` négatif métaux/actions, mém.). Indicateurs validés vs TA‑Lib (corr 1,0). | `scratchpad/strategy_tester/smc_ict.py` · `run_smc.py` · ERR‑014 |
| Nouveaux agents/edges (recherche web approfondie) | REJETÉ (rien à brancher) | deep-research 17/07 (102 agents, vérif. adversariale) : AUCUN nouvel edge directionnel prouvé net‑de‑frais au setup ; 4 pièges = daily‑pas‑intraday / brut‑de‑frais / contemporain‑pas‑prédictif / micro‑cap. Seule piste testable = flux options **Deribit** (hors clé Trade‑only, prior négatif). GAIN = **Deflated Sharpe** (déflater l'edge par le nb d'essais → mode porte `deflated` codé + ARMÉ NN/QML). NE PAS re‑lancer. **RE‑TESTÉ EN INTERACTION 18/07** : les familles de signaux du backlog §104 (réversion 7 + gates de régime KER/200‑SMA/squeeze + **momentum cross‑sectionnel #8**) réunies dans un MODÈLE JOINT GLOBAL (`scratchpad/audit_indep/global_interaction.py`, RandomForest+Ridge, WF purgé POOLED cross‑sectionnel, net frais, contrôle shuffle) → 12 configs, **net médian −3,24 bps, IC ≈ shuffle** → l'interaction jointe de TOUS les indicateurs ne crée AUCUN edge caché ; reproduit LIVE le piège n°1 (daily‑small‑sample). Confirme le bilan §104 (edge réversion réel < frais, levier = exécution) | mém. `deep-research-new-edges-verdict` · `scratchpad/audit_indep/global_interaction.py` |
| geometric v2 (topologie/dcor/nolds) | REJETÉ — **RE‑TESTÉ EN INTERACTION 18/07** | 0/14 **features ISOLÉES** sur 1m→1W ; dcor n'ajoute rien vs Pearson ; nolds prohibitif. **Interaction 18/07** (`scratchpad/geometric_v2_lab/interaction_geom.py`) : le vecteur géométrique COMPLET (Wasserstein/POT + nolds + geom_vote + corrélations cross‑asset) dans un MODÈLE JOINT (RandomForest interactions + Ridge), WF purgé, net frais, contrôle shuffle — 44 configs, **net médian −1,19 bps (< frais), IC ≈ shuffle** ; les 2 seuls « hits » = piège daily‑small‑sample (n=534/432 vs médiane 2792, signes INCOHÉRENTS cross‑symbole : 1D h4 ETH +140/SOL +117/XRP −42 bps). L'interaction ne crée aucun edge caché → **rejet confirmé sous la méthode corrigée** | `scratchpad/geometric_v2_lab/interaction_geom.py` · mém. `geometric-mirage-24h` |
| forecast Darts (NHITS/AutoETS/statsforecast) | REJETÉ | 0/32 net de frais (BTC/ETH×8TF×h1/h4) ; IC faible mangé par frais | mém. `openbb-ecosystem-verdict` · `scratchpad/openbb_forecast_lab` |
| TimesFM 2.5 (foundation model Google, zero-shot) | REJETÉ | **10/07** 0/8 TF net de frais (BTC M1→W1, 120 pts/TF, venv isolé ERR-004) ; RankIC directionnel dans le bruit (best H1 t=1.99 MAIS net −0.16 = breakeven, et seul TF qui sort parmi voisins nuls = mirage régime ; H4 net +2.4 mais IC t=0.53 = bruit) ; gross ≈ 0 → net = −12 bps (frais). Zero-shot d'un quasi-random-walk = persistance, aucun edge directionnel — conforme au prior (rendements crypto ~aléatoires + mur des frais) | `scratchpad/timesfm_lab/eval.py` · `results.json` |
| mql5 kalman_slope (Kalman price smoother) | REJETÉ | 0/34 net de frais ; IC signif. isolé XAU 1H mais < frais | mém. `mql5-codebase-tester` |
| mql5 structural break AFML Ch.17 (CSW/SADF/Chow, suite + 3 composants + SÉQUENCE) | REJETÉ | 0/40 net de frais (suite = moyenne simultanée) ; IC médian négatif (−0.02, lecture contrarienne). **Re‑test SÉQUENCE ORDONNÉE 18/07** (audit ERR‑014, `struct_break_sequence` : CSW→SADF→Chow chacun sur sous‑fenêtre + récente) : l'ordre FLIP l'IC en +0.02 (momentum) — l'interaction change bien la nature du signal — mais **0/80 net de frais t≥3**, edge minuscule mangé par les frais (conforme au prior AFML « features parallèles ») | mém. `mql5-codebase-tester` · ERR‑014 |
| filtre de conviction | REJETÉ | consensus contrarien à 1 h | mém. `edge-tuning-68` |
| gate de régime (démotion Priorité 2) | REJETÉ ×2 | réfuté en croisé | `docs/BACKLOG_RECHERCHE.md` §104 |
| carry cible 2.5 | REJETÉ (artefact) | 0.25 sur 15/15 refits walk‑forward | mém. `measurement-blind-spot` |
| voix classics (MACD/Bollinger/Donchian/VWAP/grille/pairs/funding_fade) | REJETÉ (coupée) | t ≈ −11 | mém. `edge-tuning-68` |
| voix LLM (15ᵉ) | REJETÉ (coupée) | t ≈ −4.5 | mém. `edge-tuning-68` |
| pré‑ordre (pipeline) | REJETÉ | mesuré perdant | RESEARCH_NOTES §52 |
| ema_cross / crossovers simples | REJETÉ | −83 % backtest ; WF OOS ~break‑even mais DD −65 % | `scratchpad/strategy_tester` |
| réversion court‑terme (7 signaux : momentum8/rsi14/dist_sma50/donchian20/supertrend/vortex/cmf) | REJETÉ (universel mais < frais) | **09/07** panier 21 sym × 5 secteurs (crypto‑maj/meme/métaux/actions US/ETF) × 8 TF : IC<0 (réversion) à h=1‑4 sur 1m→4H, **4/4 secteurs concordants** quasi partout ; magnitude 0.02–0.06, souvent PLUS forte hors crypto (ETF/métaux) → robustesse cross‑secteur confirmée MAIS < frais 6 bps, non tradable seul. Cross‑secteur possible seulement 1m→4H (data actions/métaux trop courte en 1D/1W). **RE‑TESTÉ EN INTERACTION 18/07** (méthode ERR‑014 : indicateurs en interaction, plus isolés) : `interaction_test.py` — (A) confluence K‑parmi‑7 + (B) slow‑momentum/fast‑reversion (arXiv:2105.13727), backtest événementiel **net de frais**, échelle TF complète, cross‑secteur, benchmark B&H, K balayé (anti cherry‑pick). Sur **500 configs : 0 forte** (net_sharpe>0,5 ∧ t≥3 ∧ net>0 ∧ bat B&H) ; médiane net_sharpe **<0 sur TOUS les TF** ; **gross≈0** aux extrêmes de confluence (0,02–0,22 bps fines, négatif ≥1H) → l'INTERACTION ne franchit pas les frais. **Rejet confirmé sous la méthode corrigée** — les 7 signaux restent des proxys redondants de « a monté » (colinéarité). | `scratchpad/audit_indep/interaction_test.py` · `signals_basket.py` |
| momentum horizon LONG (h≈96) hors crypto | REJETÉ (crypto‑spécifique) | **09/07** même panier : crypto‑maj bascule vers momentum (IC **+**) à h=96, mais actions/ETF restent en réversion → **signe OPPOSÉ, 0/4 secteurs** (ETF jusqu'à −0.13/−0.21). GARDE‑FOU : ne JAMAIS extrapoler un signal « momentum long » hors crypto | `scratchpad/audit_indep/signals_basket.py` |
| gate VPIN (toxicité du flux, Easley‑LdP‑O'Hara) — écarter les fills maker toxiques | REJETÉ — **pas d'amélioration déflation‑robuste** (claim de QUALITÉ DE FILL, distincte d'un edge directionnel) | **18/07** banc dédié `vpin_lab.py` (LECTURE SEULE, défaut OFF, classé SAFE) : VPIN par buckets de VOLUME ÉGAL (`compute_vpin`) ; buy/sell taker DIRECT (`taker_flow`) limité au snapshot car l'endpoint ne rend que **~30 barres** → backtest en **BVC** (bulk‑volume `Φ(Δclose/σ)` via `black_scholes._norm_cdf`) sur bougies `data_history/`. Expérience : tag chaque **fill maker post‑only** du VPIN CAUSAL → **markout net** (`microstructure.markout`, net 4 bps maker) `h` barres plus tard → contraste décile VPIN haut vs bas + **gate** directionnel. BTC/ETH/SOL × échelle **M1..W1** (M1 direct indisponible, W1 dégénéré — ANNOTÉS, pas fabriqués), non‑chevauchant + bootstrap + Deflated Sharpe + PBO + t déflaté. **0/92 essais robustes** : **max |t_défl| 0,47** (seuil 2,5), **max DSR 0,188** (seuil 0,95), **PBO 0,60** (choix de config surappris). Signe du contraste INCONSISTANT et plutôt à l'ENVERS (diff<0 « toxique » seult **34/92** ; intraday markout net VPIN haut **−4,39** vs bas **−6,19 bps** = haut LÉGÈREMENT MOINS mauvais, bruit). Fait structurel : markout maker **~constant** à travers les déciles VPIN (le VPIN ne stratifie PAS la toxicité). D1/W1 = ±100s bps small‑sample. Nuance : domaine natif du VPIN = temps‑volume TICK ; ici approximé au niveau BARRE (tape signée non historisée) → verdict = « avec les données DISPONIBLES, VPIN n'améliore pas la sélection des fills ». Gate NON câblé (repérage `market_maker.no_quote_reasons` / `futures_executor._place_maker`, opt‑in défaut OFF, décision proprio + mesure tick‑level requises). | `vpin_lab.py` · `docs/VPIN_LAB.md` · ERR‑001 |
| ADM / système DMI‑ADX de Wilder (+DI/‑DI, ADX>25, EMA200, SL 2×ATR) | REJETÉ (comme alpha) — **= beta long / réduction de DD** | **18/07** ⚠️ **1ère analyse FAUSSE puis CORRIGÉE (leçon méthodo, cf. `docs/AGENT_ERRORS.md` ERR‑014).** V1 (`adm_strategy.py`) évaluait les conditions SIMULTANÉMENT à la barre du croisement → « 0 trade / auto‑contradiction » = **artefact de modélisation** (l'ADX est retardé : au croisement il est au creux, il monte APRÈS). V2 FIDÈLE (`adm_wilder.py`, machine à états : Crossover→**Extreme Point Rule** (entrée au franchissement du point extrême, PAS au croisement)→ADX‑gate à l'entrée, stop‑and‑reverse). V2 TRADE (23 trades/1D, gros ret brut ETH 4H +4315 %) MAIS vs **buy‑and‑hold** : **Sharpe médian −0,31 < B&H +0,19** ; bat le B&H en Sharpe 15/47 seult ; PnL **long +587 k$ / short +5 k$ (0,8 %)** = capacité prédictive baissière NULLE, expo longue ~80 % en bull = **beta capturé, pas alpha**. Seul apport = **DD réduit** (−52 % vs −77 %). Les 15/47 cellules gagnantes (4H/1D grosses alts) = sélection de régime, à valider OOS+Deflated‑Sharpe. Correction `extreme` vs `immediate` améliore (15 vs 12) mais ne renverse rien. | `scratchpad/strategy_tester/adm_wilder.py` · `run_adm_wilder.py` |

## GARDÉ — branché, mesuré gagnant
| Idée | Preuve | Réf |
|---|---|---|
| GARCH figé (`volatility.py`) | bat `arch` 11/12 QLIKE | mém. `pypi-tools-watchlist` |
| Banc 14 agents déterministes (EARCP assaini §51) | socle, banc GELÉ à 14 (§62) | CLAUDE.md |
| régime = instrument de VOL (CVIX) | module le sizing, sain | `learning_health` |
| `fee_rates.py` — helper CENTRAL des frais EFFECTIFS (trade-rate signé + BGB effectif) | **19/07** branché et CONSOMMÉ (non dormant, ERR-013) : recâble `market_maker` (plancher spread), `listing_hype` (spot 8/10 — corrige l'ancien 6 SOUS-ESTIMÉ) et `exit_calibration` (A/R futures = round-trip taker). LECTURE SEULE, cache TTL 1 h, FAIL-SAFE = défauts en dur (taux VIP0 §1b, jamais de crash/blocage). Nuance clé : déduction BGB effective SSI option ON **ET** réservoir BGB > poussière (toggle seul ≠ remise). 5 tests (`_effective_spot_bps`, `bgb_deduction_effective` injecté, parse, fail-safe, SAFE-source) ; classé SAFE par `security_agent` ; 3 portes vertes. Corrige aussi le doc §1b (`taker_flow.py` = lecteur PUR, AUCUNE constante FEE). | `fee_rates.py` · `docs/BITGET_REFERENCE.md §1b` |

## MUET — branché mais neutralisé par la porte d'edge (mesure en ombre)
| Voix | État | Réf |
|---|---|---|
| NN fusion (16ᵉ) | armée, voix MUETTE (porte edge), ombre `nn_shadow` | mém. `llm-agent-and-no-nn-lifted` |
| QML quantique (18ᵉ) | armée, MUETTE (porte edge) ; ombre `qml_shadow` IC live +0.042 (t+9) MAIS **régime-conditionnel RÉFUTÉ 17/07** — un filtre causal (momentum `ret_60m`, seuils appris sur train) ne récupère AUCUN edge OOS walk-forward : chop −0.067 ≤ tout −0.049 ; la tendance est le sous-ensemble le MOINS mauvais (−0.019), l'inverse de l'hypothèse. Le +live = pearson pondéré-magnitude sur un split par |move FORWARD| (non causal) + fenêtre récente bénigne. **Gate VINDIQUÉ, ne pas re-tester** (`scratchpad/qml_diag/`) | mém. `qml-voice-18` |

## VIVANT — piste ouverte, à re‑mesurer
| Piste | Statut | Prochaine mesure |
|---|---|---|
| **connectivité isopérimétrique** (geometric_v2 ENRICHI 18/07) | INSTRUMENT DE RISQUE, pas edge directionnel : rétro-eng + recherche (graphes isopérimétriques, Cheeger/Fiedler ; crash-indicator PLOS 2024, market-mode RMT). Ajouté à `geometric_agent` : **market mode λ₁/N** (risque systémique) + **max spectral gap** (fragmentation/crash) + n_factors + participation + débruitage **RIE** + `connectivity_regime`. ⚠️ B-2 RÉSOLU (19/07) : le descripteur est désormais BRANCHÉ au sizing via `geometric_agent.systemic_risk_scale()` (z-score DYNAMIQUE de market_mode avec `hist` roulant persisté -> facteur ∈[0.5,1] réducteur-seulement, fail-open), consommé par `futures_auto._notional_cfg` sous le gate opt-in **`GEOMETRIC_RISK_SIZING` (défaut OFF)** — réduit la taille en co-mouvement systémique anormal, SOUS les murs, jamais >base. (Auparavant : calculé sans `hist` -> `systemic_z=None`, et non consommé — seul le VOTE directionnel geometric était câblé au banc, pas le descripteur.) ARMER = décision proprio (mesure-d'abord : observer `systemic_z` avant d'activer). Mesuré : capte le cœur crypto = 1 beta (mm 0.86) ; sur univers diversifié suit faiblement la vol (+0.08 contemporain) ; forward −0.48 MAIS n=249 = piège small-sample → **NON robuste** | à utiliser en **Z-SCORE dynamique** pour gater le SIZING (comme vol-targeting GARCH), jamais la direction · `geometric_agent.py` |
| **régime → consensus (c)** | edge banc DOUBLE en haute‑vol +0.12/+0.16 mais 1 seul bloc | re‑mesurer ~15/07 · mém. `pypi-tools-watchlist` |
| **EXÉCUTION / frais** = le vrai levier | **CHIFFRÉ 09/07** (forensics 25 trips/168h) : frais ≈ 6 bps/côté (taker plein) = **~50 % du brut** ; slippage non-enjeu (−0.1 bps) ; churn <2min = 11 % des frais (mineur) ; **maker sur les ouvertures = +27 % de net mesuré** (+0.18 $ ; exits restent taker par sécurité) → re-mesurer sur trades maker réels | `docs/BACKLOG_RECHERCHE.md` §104 · mém. `exec-fees-lever` |
| mode MAKER futures (post-only + repli taker) | **CODÉ, armé puis DÉSARMÉ le 09/07** : un `/code-review` xhigh (10 finders) a trouvé des bugs de DOUBLE-POSITION (garde anti-doublon incomplète : ne vérifiait pas l'état terminal d'annulation ni un fill sous-estimé ; clientOid du repli réutilisé → repli cassé) + un test « ancré » sur le MAUVAIS endpoint (/orders-history vs /detail). **CORRIGÉ** : garde exige un état terminal canceled/filled relu, clientOid neuf, taille au mark, lecture propre via hub._read, levier après faisabilité, allow-list security. Reste DÉSARMÉ (limit_ioc) jusqu'à re-validation EN RÉEL. Ouvertures DIRECTIONNELLES only (le carry reste taker) | `futures_executor.py` `_place_maker` · §exec-frais |
| futures edge directionnel | notional 25 $, edge robuste mais fragile | re‑mesurer ~14/07 · mém. `futures-edge-unproven` |
| **momentum cross‑sectionnel 1D** (long‑short, tri sur TOUT l'univers) | **VIVANT — SEULE piste réelle du ré‑audit 18/07 (soir).** `cross_sectional_1d.py` (12 cryptos, skip‑1, long top‑k/short bottom‑k, rebalance quotidien, net frais, shuffle+déflation) : **L=21 (~3 sem) = +16,9 bps/j net maker, Sharpe 0,89, t 2,35**, survit MÊME au taker (+13 bps/j), bat le shuffle (~0). STRESS‑TEST (`cross_sectional_1d_validate.py`) — **PASSE** : culmine à L=21 puis REDESCEND (45/60 j) = momentum Dobrynskaya, PAS trend/beta ; **market‑neutral** (corr LS↔marché −0,06 ; marché B&H +4,5 vs LS +16,9). **MAIS holdout OOS MARGINAL** : 2e moitié t=**1,28** (<2), edge diminue de moitié, et L optimal DÉRIVE (21→30). Jambe longue porte l'essentiel (12,3/16,9). **WALK‑FORWARD FAIT** (`cross_sectional_1d_wf.py`, L sélectionné OOS sur train glissant) : maker **t=1,62** (+13,4 bps/j, Sharpe 0,66), taker t=1,04 ; L fixe théorique 14 j t=1,12 → **NON significatif OOS** (le t=2,35 plein‑échantillon était gonflé par la sélection in‑sample de L). Réel, market‑neutral, théorie‑cohérent MAIS sub‑seuil → **VIVANT, PAS déployable.** | **UNIVERS ÉLARGI FAIT (12→46 coins, `expand_1d.py`)** : le plein‑échantillon se RENFORCE (t 2,35→**2,9**, confirme un effet réel) MAIS le walk‑forward OOS reste **t=1,51 maker / 0,80 taker** (inchangé vs 12 coins) — l'élargissement N'A PAS franchi le seuil. Effet **RÉEL, market‑neutral, théorie‑cohérent mais trop FAIBLE** (Sharpe ~0,6 maker OOS, taker le tue) pour être tradable SEUL. **CLASSÉ : réel mais NON TRADABLE seul** — revisiter seulement avec univers 100+ coins OU comme 1 input d'un ensemble multi‑signaux · `scratchpad/audit_indep/cross_sectional_1d*.py` |
| forecast Darts — panier diversifié → **REJETÉ 09/07** | 21 sym × 8 TF net de frais : 0/477 passe (seule UNI 1D = faux positif multiple-testing). IC RÉEL surtout actions US (0.20 t4.7)/alts (0.23 t3.5) mais mangé par frais (§104) | fait — ne pas re-tester |
| **Grok vision — lecture de chart Wyckoff/patterns** (voix d'OMBRE `grok_shadow`) | **LIVRÉ 19/07 · 1ʳᵉ MESURE 19/07 : IC≈0, PAS d'edge, ne pas armer.** 63 votes accumulés sur fenêtres HISTORIQUES look-ahead-free (Grok ne voit que le passé jusqu'au point de coupe ; forward = prix réels ultérieurs du journal overlay). `live_ic_audit` voix opt-in : rankIC −0,04/−0,07/−0,10 aux h=15m/1h/4h (t −0,32/−0,53/−0,75), pearsonIC ~0 ; contrôle horizon-fidèle (n=28) rankIC +0,23 à 15m mais t=1,19 = BRUIT. Tous |t|<2 → indistinguable du hasard. Distribution 20 long/30 short/19 neutre (72% directionnel, biais short en régime baissier). Caveat : ~12j 1 SEUL régime (baissier) → petit échantillon, re-mesurer sur plus de régimes ; **prior « lecture LLM = bruit » TENU**. `grok_vision.py` : rend un chart CHANDELIERS (candles_history) → Grok vision (xAI, OpenAI-compatible, `grok-4`) → JSON strict {phase/events/structure/patterns/bias/confidence} → croisé avec `wyckoff_lab.detect_events` (objectif look-ahead-free) → vote d'ombre `grok_shadow` dans `.overlay_votes.jsonl` mesuré par `live_ic_audit`. Outil à la demande (`--analyze SYMBOL TF`), défaut OFF, `XAI_API_KEY` requis, AUCUN ordre, ne touche pas le consensus/murs, fail-safe absolu. **Prior HONNÊTE : lecture LLM de chart = BRUIT jusqu'à preuve d'IC** (Grok hallucine ; les voix `llm`/`classics` déjà coupées t≈−4,5/−11). Rendu matplotlib PUR (mplfinance si dispo), appel via `openai`/`urllib`. Classé SAFE (`security_agent.FILES_TO_SCAN`), 10 tests hermétiques (rendu/prompt/parse/fail-safe sans clé+timeout/croisement wyckoff/bout-en-bout mocké), 3 portes vertes. | Prochaine mesure : accumuler ≥50 votes `grok_shadow` puis lire l'IC via `live_ic_audit` (bloc voix opt-in) ; ne PAS armer sur l'intuition · `grok_vision.py` · `docs/GROK_VISION.md` |

## Ré‑audit 18/07 (soir) — lentille MAKER + specs corrigées (ERR‑014, demande proprio « analyse en ensemble, pas isolé »)

Deux failles réelles corrigées avant de re‑juger : (a) specs incomplètes/fausses (`supertrend` ATR SMA→Wilder ; **momentum cross‑sectionnel mal construit** — pas de skip, formation courte → capte du reversal aux TF intraday, signe inversé) ; (b) frais **taker** seulement, jamais la lentille **maker**. Corrigé sur toutes les familles + shuffle/déflation. Recherche net : l'edge orthogonal le mieux prouvé net‑de‑frais = **lead‑lag seesaw** cross‑actif (grands→petits, négatif), PAS le funding (= carry, pas directionnel intraday).

- **réversion‑7 (crible maker `fee_sweep.py`)** : même à **0 frais, 0 config forte** (net_sh méd +0,18, ~pile‑ou‑face) ; à maker 1‑2 bps, 0 config net>0 t≥2. Le brut est trop faible pour que le maker sauve. REJET renforcé.
- **joint GLOBAL corrigé (`joint_v2.py` : Wilder + skip‑1 + lead‑lag, historique complet, shuffle+maker)** : V2 **ne bat le shuffle NULLE PART** (0/9) ; les net>0 à maker sont du BRUIT non prédictif (shuffle les bat). Correction → verdict FIABLE, pas d'alpha.
- **funding** (feature orthogonale historisée, `global_interaction_funding.py`) : contribution marginale **~nulle** (ΔIC +0,0005). Confirme carry≠directionnel.
- **geometric_v2 (crible maker `geom_fee_sweep.py`)** : 8 « pistes » flaggées = **TOUTES des artefacts** (net +75‑106 bps/**barre**, long‑horizon h=24, signes incohérents cross‑symbole ETH +0,18/XRP −0,10, null shuffle SE≈0,1). Aucun edge sous maker.
- **exits (crible frais `scratchpad/exit_fee_sweep.py`)** : la convention RÉELLE (SL1.5/RR2.0) reste **NÉGATIVE même à 0 frais** (−0,012 R). **Corrige la mémoire** : le « @4 bps maker bascule positif » était le best‑de‑grille SUR‑TESTÉ (déflaté ≤0 partout). Le SL/TP n'est PAS un levier maker.
- **orderflow (`orderflow_lab/ic_measure.py` sur alts)** : **sous‑alimenté** (endpoint ~30 barres, n≈25, |IC| valse 0,3‑0,56 = bruit, signes incohérents). Le « VERDICT: EDGE plausible (maker) » de l'outil est un **FAUX POSITIF** de petit échantillon. Non backtestable à l'horizon barre ; edge réel = sub‑minute/live (recherche B). **2 outils à durcir** (ic_measure + filtre « strong » geometric) : exiger significativité + cohérence cross‑symbole + borne de magnitude.

**Bilan** : corriger la méthode a rendu les DIAGNOSTICS justes, pas les edges gagnants. Sur specs canoniques ET sous maker ET avec shuffle/déflation, aucune famille intraday ne révèle d'edge robuste. La discipline (shuffle/déflation/cohérence/magnitude) a attrapé 3 faux positifs des outils. **Seule survivante = la piste momentum cross‑sectionnel 1D** (ci‑dessus, VIVANT).

## ÉVALUÉ — service/donnée jugé (sans mesure d'edge)
| Source | Verdict | Réf |
|---|---|---|
| Écosystème OpenBB (platform/bot/ai/forecast) | rien à brancher (data/UI, pas de signal) | mém. `openbb-ecosystem-verdict` |
| Adanos (sentiment Reddit crypto) | clé rangée ; redondant LunarCrush ; MESURER avant de brancher | mém. `adanos-koinju-services` |
| Koinju (données marché) | ne couvre PAS Bitget → hors sujet ; deribit options = piste futur (payant) | mém. `adanos-koinju-services` |
| mql5.com (articles + code base) | recoupe l'existant (topologie/quantique/Hurst/classiques) ; peu de neuf | mém. `mql5-codebase-tester` |
| **tape brute signée tick-level** (persistance `tape_store.py`) | **HISTORISÉE depuis le 19/07** (infra, pas un edge) : hook additif borné+fail-safe dans `book_collector.tick()` qui APPEND la tape `{ts,side,size,price}` par symbole (JSONL, rotation, plafond 250 Mo/sym) au lieu de la jeter. **Lève la limitation du verdict VPIN** (« tape signée non historisée » -> repli BVC-barre) : un vrai **VPIN tick-level** (buckets de volume égal, classification signée par trade) devient mesurable via `load_tape()` après ~qq jours d'accumulation. Défaut ON, gitignoré `.tape/`, classé SAFE, 3 portes vertes. Aucun edge revendiqué — socle de données à re-tester. | `tape_store.py` · `docs/TAPE_STORE.md` · `docs/VPIN_LAB.md` |
